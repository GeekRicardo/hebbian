# Hebbian 本地环境变量模板。
#
# 用法：
#   cp .env.example .env
#   # 按需填值，再启动：
#   pnpm tauri dev
#
# 加载时机：apps/desktop/src/lib.rs 的 run() 第一行调 dotenvy::dotenv()，
# 从 CWD 向上递归查找 `.env`。已有的 shell env 不会被覆盖。
# `.env` 已被 .gitignore，不会进仓库；本文件（`.env.example`）是入仓模板。

# ── 日志 ─────────────────────────────────────────────────────────────────────
# tracing-subscriber 的 EnvFilter。不设则用代码默认值（desktop = "agent_core=debug,warn"）。
# RUST_LOG=opentelemetry=debug,observability=debug,warn

# ── OTLP 通用（任何 OTLP 后端都用同一组变量） ────────────────────────────────
# OTLP/HTTP base URL（不带 /v1/traces 后缀，代码会自动拼）。
# 本地 collector：     http://localhost:4318
# Langfuse Cloud (JP)：https://jp.cloud.langfuse.com/api/public/otel
# Langfuse Cloud (US)：https://us.cloud.langfuse.com/api/public/otel
# Langfuse Cloud (EU)：https://cloud.langfuse.com/api/public/otel
# OTEL_EXPORTER_OTLP_ENDPOINT=

# OTel 标准 header 字符串，格式 `k1=v1,k2=v2`。Langfuse 用 Basic Auth。
# ⚠️ dotenvy 不接受 unquoted value 含空格（"Basic xxx" 里有空格会触发 LineParse 错误），
#    所以**必须**用单引号包裹（单引号是 literal，里面的 `=` 和空格都不会被特殊处理）。
# OTEL_EXPORTER_OTLP_HEADERS='Authorization=Basic <base64(pk:sk)>'

# 关闭 metric 导出（Langfuse 等只收 trace 的后端用）。默认开。
# HEBBIAN_OTEL_METRICS=0

# ── Langfuse Basic Auth 生成 ─────────────────────────────────────────────────
# 注意：dotenvy 不跑 shell，`.env` 里不支持 $(...) 替换，base64 必须写成明文。
# 先在终端算出来：
#   printf '%s:%s' "<public_key>" "<secret_key>" | base64
# 然后把输出粘到上面的 OTEL_EXPORTER_OTLP_HEADERS 里（单引号包裹）。
# observability 端按第一个 `=` 切 key/value，刚好对接 `Authorization` → `Basic xxx==`。

# ── 模型 IO 调试（落盘原始请求 / 响应） ──────────────────────────────────────
# 输出：~/.hebbian/sessions/<session_id>/model_io.jsonl
# HEBBIAN_DUMP_MODEL_IO=1
